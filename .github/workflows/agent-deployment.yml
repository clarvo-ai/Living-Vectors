name: Deploy LiveKit Agent

on:
  push:
    branches: [ feature/agendeployment-CI/CD ]
#    paths:
#      - 'apps/lv-pyapi/agents/**'
#      - 'apps/lv-pyapi/agents/livekit.toml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to LiveKit Cloud
        uses: livekit/deploy-action@v2.12.1
        with:
          # Changed to UPPERCASE to match documentation requirements
          OPERATION: 'deploy'
          WORKING_DIRECTORY: 'apps/lv-pyapi/agents'
        env:
          LIVEKIT_URL: ${{ secrets.LIVEKIT_URL }}
          LIVEKIT_API_KEY: ${{ secrets.LIVEKIT_API_KEY }}
          LIVEKIT_API_SECRET: ${{ secrets.LIVEKIT_API_SECRET }}
          # Passing the specific secret the agent needs
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          # SECRET_LIST tells LiveKit which of the above env vars to upload
          SECRET_LIST: "GEMINI_API_KEY"